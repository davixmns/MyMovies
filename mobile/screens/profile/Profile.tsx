import {ContainerProfile} from "./styles";
import {Text} from "react-native";

export const Profile = () => {
    return (
        <ContainerProfile>
            <Text>Profile</Text>
        </ContainerProfile>
    );
};